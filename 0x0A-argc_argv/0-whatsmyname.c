#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the name followed by new line
 * Return: 0 as success
 */

int main(int argc, char *argv[])
{
	int i = argc - argc;

	printf("%s\n", argv[i]);

	return (0);
}
