#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the name followed by new line
 * Return: 0 as success
 */

int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
		_putchar(argv[0]);

}
