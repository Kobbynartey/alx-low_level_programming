#include <stdio.h>

/**
 * main - Prints lowercase alphatet in order
 *
 * Return: (0) after code is successful
 */

int main(void)
{
	for (char c = 'a'; c <= 'z'; c++)
{
	putchar(c);
}
	putchar('\n');
	return (0);
}
